kind: pr-fix
name: pr-fix
description: "Lightweight PR fix — no architect/reviewer, just fix and push"
trigger: pr-fix

phases:
  - name: fix
    label: Fix
    prompt: prompts/pr-fix.md
    model: "{{models.pr-fix}}"
